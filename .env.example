# EvoMap-Lite 环境变量配置

# ============================================
# 数据库配置
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=evomap_lite
DB_USER=evomap
DB_PASSWORD=evomap_secret

# ============================================
# 服务器配置
# ============================================
PORT=3000
NODE_ENV=development

# ============================================
# 对象存储配置 (MinIO/S3)
# ============================================
# MinIO (推荐用于企业内网)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=evomap
S3_SECRET_KEY=evomap_secret
S3_BUCKET=evomap-bundles
S3_REGION=us-east-1

# AWS S3 (备选，需要外网访问)
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=YOUR_ACCESS_KEY
# S3_SECRET_KEY=YOUR_SECRET_KEY
# S3_BUCKET=evomap-bundles
# S3_REGION=us-east-1

# ============================================
# CI 配置
# ============================================
# CI 类型: mock | gitlab | github
CI_TYPE=mock

# GitLab CI
# CI_URL=https://gitlab.internal.com/api/v4
# CI_TOKEN=your_gitlab_token
# CI_PROJECT_ID=123

# GitHub Actions
# CI_URL=https://api.github.com/repos/your-org/your-repo/actions
# CI_TOKEN=your_github_token
# CI_WORKFLOW=validation.yml

# ============================================
# Gate Pipeline 配置
# ============================================
# 最大并发 gate 任务数
GATE_MAX_CONCURRENT=5

# Gate 超时时间（毫秒）
GATE_TIMEOUT_MS=1800000

# 自动提升阈值
GATE_AUTO_PROMOTE_CONFIDENCE_MIN=0.85
GATE_AUTO_PROMOTE_SUCCESS_STREAK_MIN=3
GATE_AUTO_PROMOTE_ENV_COVERAGE_MIN=2

# Blast radius 限制
GATE_BLAST_RADIUS_MAX_FILES=100
GATE_BLAST_RADIUS_MAX_LINES=10000

# ============================================
# 日志配置
# ============================================
LOG_LEVEL=info
LOG_FORMAT=json
